This file records major accepted milestones and new engineering progress. Detailed pre-cleanup investigation entries remain available at the named Git checkpoints in CURRENT_CHECKOUT.md; routine runs belong in artifact logs.

# Work log

- **Verilator DRAM alignment fix — 2026-09-23:** the broader memory probe exposed malloc-based host addresses that shifted aligned guest cache lines across AXI 4 KiB boundaries. The Verilator allocator now uses page-aligned storage and fails before installing an invalid pointer; ASan/UBSan alignment, size, free and injected-allocation-failure checks pass, and the host-only relink reproduces the exact known 398-cycle worker with the generated RTL archive unchanged (`logs/fpga-memory-latency-20260923/allocator-fix/`).

- **Real-DDR ten-worker measurement — 2026-09-23:** exact fixed FPGA image `93ef30cf` / RTL `97cc0932d` passes resident smoke and 16 measured guest invocations; seven alternating-order samples each give 310 worker versus 176 batch physical cycles, so the simulator’s two-cycle gap becomes 134 cycles on this board’s 64-bit DDR path. Fresh matched binaries, checksums, guest/CORE PASS and native exit 0 are audited in `logs/real-ddr-20260923/`; the initial host allocation failure was recovered with the existing home-directory outlet controller, now documented in the board skill, and no hardware waveform or broader application qualification is claimed.

- **Fixed ten-context FPGA fit — 2026-09-23:** exact top `93ef30cf` / RTL `97cc0932d` completes Vivado 2024.2 routing and verified bitstream packaging for 2 resident banks / 10 contexts / 10 prefetch slots, with 48,614 LUTs, WNS +0.694 ns and WHS +0.012 ns at 20 MHz. Exhaustive timing queries locate the 26 additional unconstrained pins entirely in existing reset-configuration tag masters; inherited constraint/RTC limitations and physical-board acceptance remain separate, with final reports and hashes in `/home/coyang/fpga-logs-context-launch-20260923/20260923T052713Z-93ef30cf/`.

- **Prefetch overlap verified independently — 2026-09-23:** fresh full-waveform decoding reproduces the 398/396 worker/batch intervals, verifies all 20 worker handoffs and cold measured data, and accounts for the two-cycle gap as 174 extra non-load cycles offset by 172 fewer load-span cycles along the same continuous prefetch response stream. Fresh clean-build worker/batch/demand reruns also reproduce 398/396/32,619 with the same simulator executable, ELF, and NBF; demand serializes 160 ordinary reads while the two prefetch schedules reach ten outstanding, so its ratio includes transport differences, with the detailed audit and timeline retained in `logs/context-launch-20260922/verification-200-analysis/`.

- **Accepted completion preserved across switch flush — 2026-09-23:** the new cold-load failure is caused by a nonspeculative late-writeback injection accepted on the same edge as switch commitment, then killed by an unconditional context-switch validity clear; the source slot is still correctly owned. An isolated calculator fix preserves nonspeculative packets while retaining queue flushes, and the exact formerly failing ELF passes all 24 trials with 72 verified nonresident handoffs and correct source-image writes; all four targeted translated/resident regressions and the 13 harness checks also pass. Fresh bug-fixed worker/batch runs retain independently audited 398/396 cycles; exact RTL `97cc0932d` and the maintained regression are integrated locally on `fix/nonresident-completion-preservation-20260923` (`logs/context-launch-20260922/late-result-fix/`).

- **New nonresident late-result reproduction — 2026-09-23:** an isolated two-bank/four-context directed test fails on checkpoint RTL `952ec7cb5`: an independent cold load to x15 immediately before a nonresident switch restores the old sentinel after a `0→2→3→0` round trip. The closed failing trace, exact source, binary, and manifest are retained in `logs/context-launch-20260922/gate-nonresident-late-result-baseline/`; localization is in progress, and further performance-variant acceptance is deferred.

- **Final-line redirect overlap validated — 2026-09-23:** a fresh traced baseline reproduces 408 worker cycles with ten overlapping prefetches, zero outstanding work at the timed boundary, and no ordinary data refill. Capturing the frontend redirect on the final integer restore edge passes the identical-ELF worker at 398 cycles; the initial predicate added a combinational-loop warning and was replaced by an equivalent registered-valid predicate, restoring baseline warning counts. Matched batch/switch-only controls pass at 396/247 cycles; all five traced qualification gates pass, including resident/nonresident overhead of 5.13/9.26 cycles per switch. RTL `952ec7cb5` is checkpointed on `perf/overlap-context-launch-20260922`, with cross-latency and exact-endpoint routed checks pending; source snapshots, closed traces, and audits are in `logs/context-launch-20260922/`. The candidate trace shows all eighteen nonresident handoffs shorten from eight to seven cycles, while the first consumer's load replay interval grows from 83 to 91; this accounts for the net ten-cycle improvement. Printed and committed-PC intervals agree at 398, with ten overlapping fills and zero ordinary data refills.

- **Early context-line restore — 2026-09-21:** allowed the first valid integer context line to write into the drained physical register bank on the same edge that `wait_drain` transitions into `save_restore_regs`. The write is gated by backend drain safety, a valid prefetched line, and nonzero integer dirty state; the existing FSM path remains for all other cases. Exact full-system runs pass with worker `328` cycles at 40-cycle reads (down from `346`), `408` at 200-cycle reads (down from `418`), and `608` at 400-cycle reads (down from `618`); matched batch remains `396` at 200 cycles, and switch-only falls from `289` to `271`. Focused UCE, 37 analyzer, and 13 harness tests all pass. This saves one restore bubble per ring while preserving the demand-join behavior.

- **Frontend-accept fast-exit probe reverted — 2026-09-21:** exposed the FE's actual I-cache redirect acceptance pulse and allowed the backend context-cache FSM to leave `done` on that pulse. The exact 200-cycle worker benchmark remained `418` cycles with guest/host checks passing, so the `done` state is not exposed on the measured critical path. The four-interface RTL change was reverted; no performance claim is retained.

- **Compact worker-register probe reverted — 2026-09-21:** replaced temporary `t3/t4` registers with `t0/t1` so the measured worker appeared to touch only one 16-register line. The exact 200-cycle full-system run still measured `418` cycles with all checks passing, identical to the validated worker. The register-line count is therefore not the critical residual in this schedule; the benchmark-only assembly change was reverted.

- **Dedicated switch-control comparison — 2026-09-21:** the standalone nonresident overhead benchmark passes with resident/warm switches at `5.13` cycles each and nonresident/cold switches at `11.26` cycles each (`6.13`-cycle cold penalty). The ten-worker switch-only ring averages `289/20 = 14.45` cycles per handoff, so it includes the CSR handoff instruction, frontend scheduling, and loop boundaries in addition to the isolated context-cache transaction. This directly invalidates the original additive estimate that treated 14.45 cycles as pure switch latency.

- **Wide context-line probe reverted — 2026-09-21:** tried changing the nonresident integer context image from two 16-register lines to one 32-register line to remove a restore cycle. The full-system 200-cycle worker run compiled but hung for hours without reaching the benchmark pass marker, consistent with an address/valid pipeline mismatch in the widened synchronous context memory. All four RTL edits were reverted; no performance or correctness claim is retained. The validated 16-register-line implementation remains unchanged.

- **Context-cache FSM breakdown — 2026-09-20:** streamed the exact 200-cycle worker waveform through `tools/ctxtsw_vcd_stream_events.py`. Repeated nonresident handoffs spend 3 cycles waiting for CSR-switch commit, 2 draining the backend, 2 transferring the dirty integer line, 1 launching the frontend, and 2 waiting for frontend acceptance: a ten-cycle active context-cache transaction. The worker body then contributes about eight idle cycles before the next handoff. The first consuming worker has an 84–85-cycle idle interval while its detached fill becomes visible; later workers do not repeat it. This identifies the remaining floor as real context-cache/FSM work plus the first-fill tail, rather than the old duplicate refill. Detailed timestamps and the exact command are in [`logs/rtl-context-breakdown-20260920`](logs/rtl-context-breakdown-20260920/README.md).

- **Latency sweep and scheduler-round probe — 2026-09-20:** with the demand-join RTL, the worker/batch relationship is latency-dependent rather than a fixed switch tax. At 40-cycle reads, worker/batch/switch-only are `346/236/289`; at 200 cycles they are `418/396/289`; at 400 cycles they are `618/596/289`. The worker therefore has a roughly fixed 289-cycle handoff floor, while batching spends more of the interval hiding memory latency; the worker gap is 110 cycles at 40-cycle reads but only 22 cycles at 200 and 400. Adding one extra cooperative ring (`BARRIER=1`) raises the 200-cycle worker to 491 cycles, so software delay is not a useful way to hide the first 85-cycle resume-to-load window. This supports the interpretation that the RTL join removed the duplicate refill, while the remaining low-latency gap is actual nonresident scheduling work and the high-latency residual is the first-fill/hand-off tail.

- **RTL demand join validated — 2026-09-20:** changed the UCE request-acceptance boundary to hold a same-line demand miss while its detached prefetch slot is live. The dcache can then recompute its tag hit and withdraw the stale miss before a duplicate ordinary refill is issued. The focused ten-slot UCE regression passes. On the exact two-resident/ten-logical-worker full-system benchmark with ten AXI read slots, the 200-cycle model measures demand `32,661`, worker prefetch/yield/load `418`, matched batch `396`, and switch-only `289` cycles; the 400-cycle model measures worker `618`, matched batch `596`, and switch-only `289`. Guest and host checks pass in every run; the simulator still exits through the known nonfatal GPIO teardown assertion after `CORE PASS`/`BSG PASS`. The RTL join therefore removes the prior 3,329-cycle duplicate-refill stall across both latency points: worker overhead above matched batch is 22 cycles in each case. Relative to the measured 200-cycle demand row, the worker is 78.14x faster. This is a simulator-validated RTL candidate; FPGA synthesis and physical-board qualification remain separate follow-up checks.

- **Latency-coverage barrier experiment — 2026-09-20:** added an optional `BARRIER=N` benchmark knob that performs extra cooperative handoff rounds after each hint and before its load. At 200-cycle reads, `BARRIER=2` reduces the production-layout worker from 3,645 to 492 cycles; the same ELF's batch row is 737 cycles. A traced barrier run has ten outstanding prefetches, ten complete eight-beat fills, and zero matching normal misses for every line. At 400-cycle reads, `BARRIER=2`, `4`, and `16` all remain at 7,039 cycles, so the fixed ring count is latency-sensitive and is not a universal default. The option remains disabled by default; the result demonstrates that scheduler delay can cover the stale-metadata window, while an RTL demand join is still the path to remove that delay.
- **Ready/valid trace-check correction — 2026-09-20:** the prefetch analyzer previously treated `prefetch_response` (valid) as if it were a consumed handshake and rejected a legal response held during shared L1 data/tag-port backpressure. It now advances transaction state only on `fsm_rev_v & fsm_rev_yumi`; the existing 37 analyzer tests pass, and the barrier trace validates as ten complete fills with no normal demand matches.
- **Post-resume refresh probe — 2026-09-20:** tested an optional software re-hint immediately after each worker resumed, before its consuming load, to see whether stale miss metadata could be repaired without RTL changes. At 200-cycle reads the diagnostic worker result increased from 3,677 to 6,948 cycles; the original 3,329-cycle first resume gap remained and a second long gap appeared later in the ring. The extra hint therefore adds traffic and does not join the already accepted demand miss. The probe was removed; no performance claim or production option is retained.
- **Duplicate-refill timing — 2026-09-20:** correlated the accepted worker trace's consuming-load markers with its AXI transaction list. The first worker's demand miss is accepted at cycle 1,427,098; the matching detached hint response arrives at 1,427,150, but the demand metadata is already captured. The UCE consequently issues a second ordinary line read at 1,427,165; its first beat returns at 1,427,574 and its eighth/final beat completes at 1,430,402. The next measured consuming load follows at 1,430,420. This single duplicate eight-beat refill accounts for the 3,329-cycle first resume gap; the remaining nine loads are separated by 5-cycle consume work and roughly 16–22-cycle handoffs.

- **Resume-boundary decomposition — 2026-09-20:** extended the optional diagnostic ELF to record the cycle immediately after each worker resumes from its handoff. At 200-cycle reads, the worker resume-to-load delays are only 8,5,5,5,5,5,5,5,5,5 cycles; resume gaps are 3,329,22,15,22,22,22,22,22,22. The matched switch-only control has resume gaps 23,16,9,20,16,16,16,16,16. Thus the worker's large interval is one redundant demand refill holding the next handoff, not twenty handoffs each costing 132 cycles. A one-extra-ring software barrier was also tested and left a 3,273-cycle first gap, so delaying by one ring is insufficient at this transport latency and is not retained.
- **Demand-join experiment — 2026-09-20:** exercised a prototype that reused the dcache refill `critical/last` protocol for a demand whose detached hint was still live, plus a bounded recent-fill replay for hints that retired just before demand acceptance. The isolated UCE regression passed. Full-system 40-cycle and 100-cycle runs completed (`346` and `366` cycles with diagnostic/layout differences), and the 200-cycle run completed functionally at `3,655` cycles with the first long stall removed; load completions were separated by `78`, then `3,252`, then roughly `20` cycles. The ten-slot recent-fill probe showed live merging for line 0 and completed-fill replay for line 1, while line 2 still escaped the replay window. Because the prototype was not yet a complete all-worker demand join and required an unverified dcache arbitration change, all RTL changes were reverted. This is retained as mechanism evidence, not a performance claim.
- **First-consumer diagnostic — 2026-09-20:** added an opt-in `DIAGNOSTIC=1` mode to the matched ten-worker ELF that records each consuming-load completion cycle without changing the default benchmark layout. At 400-cycle synthetic read latency, the worker's first load is followed by the other nine only 6,718 cycles later, while the remaining loads retire about 13–20 cycles apart. The accepted trace correlates this with line 0: its detached fill completes, but the already-accepted demand miss immediately issues a second normal read because its miss metadata was captured before the prefetch tag became visible. A one-ring barrier changes the result by only 10 cycles, so the residual is a stale-metadata/cache-request interaction rather than a fixed context-switch tax. A cache-boundary hold experiment deadlocked because the live demand blocks the serialized cache request stream; it is not retained. The diagnostic remains optional and the production default is unchanged.
- **Matched batch validation — 2026-09-19:** one runtime-selected ELF with common context/result setup, fixed all-mode instruction warmup, separate cold measured data, traffic drain, committed BEGIN/END PCs, and untimed checks passes the accepted minimal-top sweep at 32,661 demand, 3,645 worker prefetch, 724 batch, and 289 switch-only cycles. The worker is 8.960x faster than demand and 403.453% slower than batch; the accepted worker trace has zero outstanding UCE/AXI work at BEGIN, ten outstanding UCE and tagged AXI requests before the first response, ten consuming loads, and a printed/committed interval match. The earlier 35,893/6,877/3,955/3,521 sweep is rejected because a cold instruction refill crossed BEGIN (`logs/matched-prefetch-20260919/`).
- **Ideal-reference audit — 2026-09-18:** disassembly confirms that the candidate retains all hint, load, result-store, and switch instructions; the closed waveform contains twenty committed switches, ten distinct nonzero-ID AXI reads, and the checked worker results. The raw 1,038 candidate / 1,030 batch difference is nevertheless confounded. The batch timer begins about 450 cycles before its first hint while ordinary and cold instruction traffic completes, versus about 12 cycles for the ring; that roughly 438-cycle skew nearly cancels the 472-cycle switch-only row. The batch also starts admitting its remaining nine hints only after the first completes. The supported performance claim is 2.662x over the matched 2,763-cycle demand ring. A common binary, identical untimed setup, warmed instruction paths, drained traffic, explicit timing markers, and equivalent bookkeeping are required before claiming a percentage from ideal.
- **Full-top ten-way prefetch overlap — 2026-09-18:** added an opt-in path from detached data-UCE hints through `bp_unicore` and `bp_processor` to a dedicated AXI bridge in `bp_axi_top`. Prefetches bypass the blocking L2 DMA transport, use AXI IDs 1–10, and reassemble interleaved 32- or 64-bit responses; ordinary traffic retains the existing ID-zero path and demand wins AR arbitration. On the exact full top with 200-cycle reads, fresh boots measure 2,763 demand, 1,030 separately compiled batch reference, 1,038 ten-worker prefetch/yield/load, and 472 switch-only cycles. The matched ring comparison is 2.662x over demand; the ideal-reference audit above disqualifies the raw 0.777% gap as an ideal-distance claim. The final trace proves ten UCE reservations, ten concurrent AXI reads, IDs 1–10, and nine resumed loads with no later normal miss. Enabled and disabled full-top builds, focused UCE/bridge regressions, 28 analyzer tests, and 13 harness tests pass. Immutable candidate top `154554ae`, BlackParrot `bf65c54fa`, and subsystem `93e88366` are pushed. FPGA job `20260918T081302Z-154554ae` routes at 49,197 LUTs (92.48%), 28,065 registers, 83.5 BRAM tiles, and 11 DSPs, with WNS/TNS +0.755 ns/0, WHS/THS +0.037 ns/0, and zero bitstream DRC errors. Verified package and bitstream SHA-256 values are `65ce394e07a5d176ccc1c94bb767d396c265f8920fec45a66bbb377a2a1d0316` and `58d8dc8b945d6db67fc3eb666f3183a3b4670d007e94e1714204802416c46082`; board qualification remains pending.
- **Exact ten-worker FPGA endpoint routes — 2026-09-18:** expanded the experimental static endpoint to two resident banks, ten SRAM-backed logical contexts, and ten detached prefetch slots. The exact full-system model runs the ten-worker prefetch/yield/load program through all worker checks, `CORE PASS`, and host `BSG PASS`. Area-flow job `20260918T064050Z-9e4b021d` routes at 48,645 LUTs (91.44%), 28,029 registers, 83.5 BRAM tiles, and 11 DSPs, with final WNS +0.251 ns, TNS 0, WHS +0.023 ns, THS 0, and zero bitstream DRC errors. The verified package SHA is `5b30e21e92d7693133c3e65e2e53721dc40dc1ea2d65faa7f94c1c07245f8b84`, bitstream SHA is `b7e05606979f79c0bf759c3a69fd10705d13ec026d85e2d2e621c643f0555a47`, and exact benchmark NBF SHA is `56878463ea4e8d3d217e5a26712b47add85d4ded9cdea9770231c86ef4adef28`. Board qualification remains pending.
- **Ten-entry FPGA endpoint routes — 2026-09-18:** the static ten-entry configuration routes with the named Vivado `Flow_AreaOptimized_high`/`Area_Explore` flow in job `20260918T055255Z-bcf24d47` at 46,033 LUTs (86.53%), 22,908 registers, 83.5 BRAM tiles, and 11 DSPs. Final timing passes at WNS +0.219 ns, TNS 0, WHS +0.019 ns, and THS 0; bitstream DRC reports zero errors. The locally verified package SHA is `cd0e585186e40eee0496926ff392d5faf9133650e1ab4bbb3387e6fa45eea479` and bitstream SHA is `d2225b8318d6e4fdb74ecd7b6c35b8efcc1e792bb77113eab9335e2662d66757`. This four-logical-context fit image is not board-qualified and is superseded for the original experiment by the exact routed endpoint above.
- **Ten-entry FPGA fit boundary — 2026-09-18:** added a separate static ten-entry endpoint so the accepted four-entry configuration remains stable. The response-ID-optimized default-flow control (`90b03296` / `cc8297dec`, job `20260918T045847Z-90b03296`) synthesizes to 54,081 LUTs but fails placement at 11,535 required slices versus 11,333 available. Removing reset from validity-gated slot payloads preserves the exact 10,083-cycle result and strict ten-way UCE/AXI overlap, but job `20260918T052936Z-ef50644f` regresses to 54,105 LUTs and 11,551 required versus 11,335 available; that experiment is not retained. Hierarchical reports show the direct UCE growth is only 75 LUTs and 224 registers; global remapping in the near-full default flow causes most of the design-level increase, motivating the accepted area strategy rather than an unmeasured RTL rewrite.
- **Routed-capacity simulator comparison — 2026-09-18:** parameterized the ten-worker runner's prefetch-entry count and added it to the Verilator model identity. Fresh four-entry, 200-cycle runs measure 35,856 demand versus 32,624 prefetch/yield/load cycles, a 1.099x speedup and 9.014% reduction. The matching ten-entry candidate remains 10,083 cycles (3.556x); with only four nonblocking slots, six of the first-lap hints cannot remain outstanding. This isolates the performance cost of the four-entry PYNQ-Z2 fit decision; board qualification is still pending.
- **Parameterized prefetch endpoint routes — 2026-09-18:** replaced the hardcoded ten-entry data-UCE override with `dcache_prefetch_els`, made the simulator model identity include that value, and made the ten-worker target request ten explicitly. The UCE now decodes echoed response IDs directly instead of synthesizing a ten-way response-address CAM; assertions still require a live slot and exact address. The ten-slot simulator remains exactly 10,083 cycles, while the four-slot PYNQ endpoint at top `e66721e2` / RTL `cc8297dec` routes in job `20260918T041813Z-e66721e2` at 51,791 LUTs (97.35%), WNS +2.178 ns, TNS 0, WHS +0.035 ns, and THS 0. Board qualification remains pending.
- **Synthesis farm configuration identity — 2026-09-18:** the farm controller previously forced the base FPGA configuration even for an admitted prefetch build. The incorrect job was canceled before producing evidence. Admissions now store, print, pass, and record the selected FPGA configuration; a shell-level positive/default/invalid-value check passes, and the replacement job records `e_bp_unicore_zynqparrot_prefetch_cfg` in every manifest.
- **Original ten-worker result achieved — 2026-09-18:** all ten logical workers now execute one hot body while retaining private data addresses, continuations, and eight nonresident contexts. With 200-cycle reads, fresh-boot demand/prefetch/batched-reference/two-ring-switch runs measure 35,856 / 10,083 / 13,160 / 3,482 cycles. The cheap-context candidate is 3.556x faster than demand and 23.38% faster than the immediate single-thread prefetch-then-load sequence. The trace proves ten UCE slots and ten AXI reads outstanding, with all ten hints admitted before the first response; nine later loads hit and context zero joins its pending fill.
- **Benchmark and final-beat correctness — 2026-09-18:** the old million-cycle candidate number was invalid because an inline-assembly call let the shared worker overwrite the start timestamp in call-clobbered `t1`; an ABI-visible call now preserves it in `s2`. Result bookkeeping was moved away from measured L1 sets. The UCE now retains a prefetch slot until the final response beat is accepted, and a focused five-cycle final-beat backpressure regression passes alongside the MSHR, AXI bridge, 28 analyzer, 13 harness, and 26-program compile gates. BlackParrot `43015ad30` is pushed on `master`; top code checkpoint `5b3e4344` is pushed.
- **Ten-slot FPGA area limit — 2026-09-18:** isolated full-system job `20260918T023254Z-7fb976ac` passed synthesis but failed PYNQ-Z2 placement at 55,781 combined LUTs versus 53,200 available and 11,528 required slices versus 11,306 available, with 383 control sets. This job uses the full `cosim/black-parrot-example` system and does not qualify the separate minimal AXI bridge. FPGA deployment now requires area reduction before reroute and board gates.
- **Initial pipelined prefetch transport — 2026-09-17:** the minimal Zynq top routes detached 64-byte hints through a ten-ID AXI burst bridge while preserving the ordinary ID-zero path, locks AR arbitration under backpressure, accepts interleaved response IDs, and consumes delayed dcache replacement metadata without contaminating demand state. Focused UCE/MSHR/bridge gates and the strict waveform gate passed. The original distinct-worker measurement reached only two UCE and three AXI requests outstanding; it was superseded by the shared-worker result above.
- **Mixed-topology compile gate — 2026-09-17:** the complete program build previously compiled the ten-logical-worker benchmark with the global four-context default and failed its required topology check. The `all` target now compiles maintained gates at 2/4 and invokes that benchmark at 2/10; the README states the mixed-topology behavior.
- **Detached prefetch across nonresident handoff — 2026-09-14:** RTL `9e06fbd34` accepts hints into the ten-entry UCE table while the demand FSM waits, allows hint responses to coexist with ordinary refill traffic, and lets the context handoff drain ordinary credits while detached hint credits remain live. The focused UCE and MSHR regressions pass. The normal ten-worker simulator run improves from `0x56e22` (355,874) demand to `0x56d07` (355,591) prefetch cycles; the 200-cycle pipelined stress run remains slightly slower (`0x110961` versus `0x10fe42`). This is overlap capability evidence, not a near-ideal speedup claim.
- **Minimal memory serialization localized — 2026-09-14:** the closed high-latency trace proves a maximum of three outstanding UCE hints, usable L1 fills for nine of ten later loads, and only one outstanding AXI read. Static elaboration confirms the minimal system contains no L2 and uses the explicitly single-request `bp_axil_master`; the ineffective `BP_L2_BANKS`/`BP_L2_SETS` experiment was removed, and the next implementation boundary is a bounded multi-outstanding BedRock-to-AXI adapter.

- **FPGA/Linux acceptance — 2026-09-06:** top `032420c3` / RTL `25089713` fit the PYNQ-Z2 at 46,851 LUTs, 80 BRAM tiles, WNS +1.781 ns, and TNS 0; Linux ran the PID-1 U-mode 0→2→0 handoff, target syscall, and register-restoration checks through `CORE[0] PASS`. The physical benchmark measured 5.10 resident and 11.12 nonresident cycles/switch; [the checkout guide](CURRENT_CHECKOUT.md) retains exact identities.
- **Canonical checkout — 2026-09-06:** `91fce0dd` consolidated the accepted RTL, matching tests, and host/farm safeguards into `zynq-parrot/import/black-parrot`; clean traced translated handoff and resident smoke passed, with the known post-PASS GPIO teardown assertion.
- **Recoverable directory cleanup — 2026-09-06:** `e88503c6` records archival and removal of 114 old local worktrees, preserving dirty work and evidence while recovering roughly 14 GB. See [recovery instructions](CURRENT_CHECKOUT.md#history-and-recovery); no remote VM or board state changed.
- **Documentation/history cleanup — 2026-09-06:** superseded plans and diaries now live at the published pre-cleanup Git checkpoint, current guides are concise, and 120 obsolete local branch names have exact-tip archive tags. The RTL documentation commit `5d5646ff9` leaves hardware unchanged; top-level integration preserves that tested gitlink rather than importing divergent RTL-master changes.
- **No measured performance regression — 2026-09-06:** a clean traced two-resident/four-logical model passed translated data handoff and resident smoke, and two benchmark runs exactly matched the earlier simulator's reported 5.13 resident / 11.13 nonresident cycles per switch. Evidence is in `logs/docs-integration-20260906/`; guest PASS is distinct from the known post-PASS GPIO teardown assertion, and no new synthesis or board result is claimed.
- **Reviewable fork integration — 2026-09-06:** replaced the oversized squash with five top-level and three RTL commits, retained 13 focused tests instead of 27, removed 22 historical/duplicate diagnostic tools, and consolidated current guidance into six root documents. All 118 obsolete remote branch tips were published as archive tags before pruning; final hardware is unchanged, all 13 tests compile, and fresh traced handoff/smoke plus two 5.13/11.13-cycle benchmark runs pass under the trimmed harness (`logs/reviewable-integration-20260906/`, known post-PASS GPIO assertion unchanged).
- **Finer review history — 2026-09-06:** split the accepted endpoint into twelve top-level and seven RTL commits, separating tests, operational tools, storage primitives, independent correctness fixes, and measurement; the previous series is preserved in both forks at `archive/pre-logical-split-20260906`. Intermediate RTL elaboration and tooling checks pass, and a fresh clean build, handoff/smoke tests, and two unchanged 5.13/11.13-cycle benchmark runs validate the byte-identical final hardware (`logs/refined-history-20260906/`, `logs/commit-split-validation-20260906/`; known post-PASS teardown assertion remains).
- **Stable integration — 2026-09-07:** integrated FPGA/Linux-accepted RTL `6c97bcc0a` and its 46-case register-target regression without the exploratory application benchmarks; SQLite remains recoverable at `archive/sqlite-progress-screen-20260907`. All 14 programs compile and four clean-model traced gates pass, retaining 5.13/11.13 resident/nonresident cycles per switch (`logs/stable-master-20260907/`; existing post-PASS GPIO teardown assertion remains).
- **Verification hardening — 2026-09-07:** fixed stale-log acceptance, destructive default Make behavior, and incompatible artifact reuse; ten isolated unit-test groups verify failed-build rejection, strict success/error handling, safe defaults, and configuration changes. The checkout guide explicitly limits support to the validated cooperative integer-context platform rather than claiming general production-ready threading.
- **Shell-launched Linux handoff — 2026-09-07:** transferred a separately compiled ELF into running FPGA Linux, verified its checksum, and executed the 0→2→0 register-restoration test from BusyBox with PASS, exit 0, a still-usable shell, and clean poweroff (`logs/linux-shell-demo-20260907/`). The PID-1 acceptance binary is unchanged; short transfer commands avoided an earlier pre-test BusyBox crash, and this demo remains limited to one launch per fresh boot.
- **Linux benchmark — 2026-09-07:** a shell-launched nonresident 0↔2 benchmark passed seven 256-switch samples, with median 11.140625 cycles/switch and maximum 11.15625, then returned to a usable shell and powered off cleanly (`logs/linux-shell-benchmark-20260907/`). These are loop-inclusive hardware handoff measurements under Linux, not Linux scheduler costs; resident-context Linux initialization remains outside the validated scope.
- **Resident initialization reproduction — 2026-09-07:** a new translated U-mode resident test confirmed that first seeding leaves the inactive CSR bank in reset state; initializing it fixes translation and both ECALLs, while a separate inheritance/isolation/reseed test passes. The stronger test also exposed cold I-cache refill metadata selecting register bank 0 despite resident context 1 running; a separate narrow tag-preservation fix is under local verification (`logs/resident-csr-init-20260907/`), with FPGA/Linux acceptance still pending.
- **Resident replay root cause — 2026-09-07:** after repairing refill ownership, the same test exposed a rollback reading an instruction from the next queue pointer but its register-bank tag from the old head, causing a misaligned store. The traced fix stores each instruction with its tag in the existing preissue memory; no FPGA build has been spent on these locally failing candidates (`logs/resident-csr-init-20260907/fixed-refill/`).
- **Resident local fix validation — 2026-09-07:** RTL `83fc32d29` adds first-seed CSR installation and `aad56bd92` preserves refill/replay register-bank ownership; the identical regression ELF now passes translated resident execution, both U-mode ECALLs, and private/restored register checks after failing on the baseline. The clean traced run retains the known post-PASS GPIO teardown assertion; FPGA/Linux and broader regression acceptance remain pending (`logs/resident-csr-init-20260907/fixed-replay/`).
- **Readiness cleanup — 2026-09-08:** hardened program-specific verdicts, unexpected-trap rejection, ring/benchmark peer evidence, Linux ELF freshness, and waveform context IDs; all 15 traced 2/4 programs and 21 host checks pass, and deliberate no-op/late-trap controls establish failure detection. Archived 46 retired or superseded test artifacts with verified hashes and removed obsolete FPGA recipes; the original benchmark still reports 5.13/11.13 cycles per switch, while the hardened new ELF reports 5.12/11.32 (artifacts in `logs/readiness-cleanup-20260908/`, known post-PASS GPIO teardown unchanged, current resident FPGA/Linux acceptance still pending).
- **Resident FPGA/Linux acceptance — 2026-09-08:** after an authorized board power cycle and verified overlay reload, RTL `aad56bd92` passed shell-launched resident 0↔1 and nonresident 0↔2 rings, first-seed/reseed checks, private/restored registers, target syscalls, exit zero, a usable shell, and clean `CORE[0] PASS` poweroff. Seven 256-switch samples give median 5.10546875 resident / 11.14453125 nonresident cycles per switch; exact artifacts and closed transcripts are in `logs/linux-resident-shell-20260908/`, and the accepted RTL plus verified cleanup are integrated and pushed to both masters.
- **Load-ahead qualification — 2026-09-08:** the new faulting `lbu x0` helper, resident data/register test, and four-schedule benchmark pass on the unchanged accepted FPGA RTL and a clean traced simulator; 19 analyzer and 16 existing host checks pass. Traces establish full-refill overlap while only same-context load-ahead hides the simulator critical-beat wait; FPGA totals for 16 lines are serial 1675, same-context ahead 1173, resident control 1882, and resident ahead 1465 cycles (one fixed-order series, evidence in `logs/resident-cache-overlap-20260908/`, simulator GPIO teardown unchanged).
- **Independent-request benchmark correction — 2026-09-08:** corrected the application specification to retain the original pinned OS-thread no-prefetch baseline, batch-10 ideal, and per-worker prefetch/yield/consume candidate without arithmetic padding. Existing load-ahead results remain mechanism evidence only; the next trace must test acceptance of another worker's memory request during a pending miss, with matched two-worker comparisons distinguished from the ten-worker reference.
- **Independent-request boundary — 2026-09-08:** the new two-resident request benchmark passes traced simulation and FPGA checksum/count/completion checks, with known-good resident smoke also passing. Analysis of 768 cold misses shows all 756 within-page pairs serialize admission after the preceding full refill; peer instruction dispatch during refill does not establish concurrent fetches (`logs/independent-requests-20260908/`, exact results in PAPER_DIRECTION.md).
- **Nonfaulting prefetch checkpoint — 2026-09-08:** pushed top `848e6ee7` / RTL `f284f766b` with two UCE hint slots and an equal-capacity two-bank L2; nine full-system gates and all 21 program builds pass, and traces prove concurrent opposite-bank cold reads in batch2. The resident schedule still serializes backing-data reads behind L2 response ordering; FPGA routing and Linux application qualification remain pending (`logs/nonblocking-prefetch-20260908/`).
- **Concurrent resident prefetch validation — 2026-09-08:** per-bank L2 response metadata removes the measured 47-cycle head-of-line delay while preserving ordinary response order; nine full-system gates and a directed real-controller regression pass, and identical benchmark binaries now show 12 overlapping cold-data prefetch pairs per resident page with median 3867 versus 5079 control cycles (three simulator samples). RTL `f7eedd955` additionally fixes a Vivado-only implicit-net declaration-order issue; old/fixed synthesis reproduces and eliminates it, final clean benchmark/smoke results agree, and replacement routed job `20260908T223358Z-fd5a7872` is pending (`logs/nonblocking-prefetch-20260908/`).
- **Prefetch FPGA qualification — 2026-09-08:** top `fd5a7872` / RTL `f7eedd955` routes on PYNQ-Z2 with 51,334 LUTs (96.49%), 81 BRAM tiles, WNS +2.905 ns and no setup/hold violations; the exact verified image passes all six selected bare-metal gates, including resident prefetch/yield/load and translated nonresident handoff. Three physical-board samples give median 2398 versus 3164 matched-control cycles; Linux shell acceptance remains pending (`logs/nonblocking-prefetch-20260908/`, the failed detached launch produced no target result and was recovered before the accepted foreground runs).
- **Prefetch Linux acceptance and integration — 2026-09-08:** the exact prefetch FPGA image passes the hash-verified shell resident/nonresident benchmark, target register/syscall checks, exit zero, a subsequent shell command, and clean poweroff; all seven raw samples match the previous image, retaining median 5.10546875/11.14453125 cycles per switch. The qualified RTL and collateral are integrated into both masters; Linux request-benchmark commit `a6b2349d` stays on its feature branch until the second resident-launch lifecycle is fixed (`logs/nonblocking-prefetch-20260908/linux-shell/`).

- **Resident launch ordering reproduced.** A validated same-PC Linux operand capture confirms the second launch supplies context 1 / VA `0x11cb8`. A focused translated regression first exposed an independent adjacent NPC-seed/switch hazard: the FE captures reset PC/privilege/translation one cycle before the seed commits, then fetches untranslated; closed traces and original ELF/NBF are retained in `logs/linux-request-lifecycle-20260908/`.
- **Remote seed writeback conflict fixed locally.** A source `t3` writeback discarded by a simultaneous CSR802 remote write is retained after full seed serialization; the exact failing register-form binary, nine full-system gates and 13 harness checks pass, with unchanged raw prefetch benchmark cycles. RTL checkpoints `929e5d650` and `d18f16849` preserve both fixes and their collision assertions; the cold-fetch trace does not justify an APC change, while FPGA/Linux qualification remains pending (`logs/linux-request-lifecycle-20260908/`).
- **Resident interrupt PC fixed locally.** A controlled pending CLINT interrupt before the reseeded target retires saves its old parked PC on `d18f16849`; RTL `f6e004283` updates only inactive resident APC storage, and the identical binary now saves and returns through the new entry at the same interrupt cycle. Six final traced gates and 13 harness checks pass, all 24 programs compile, prefetch cycle rows are unchanged, and final warning counts match the prior model; routed FPGA and original Linux qualification remain pending (`logs/linux-request-lifecycle-20260908/local-reseed-apc-final/`).

- **Linux request qualification — 2026-09-08:** routed RTL `f6e004283` passes all nine physical FPGA gates and the unchanged Linux request executable, including its previously failing one-request case and seven measured two-worker samples with clean shell return/poweroff. On the 2 MiB, 4,096-request-per-worker workload, resident prefetch takes median 629,204 cycles versus 613,012 without hints (2.64% more); this does not reproduce the separate tiny bare-metal speedup (`logs/linux-request-lifecycle-20260908/{board-qualified,linux-one-request,linux-matched2}/`).
- **Ten-worker reference — 2026-09-08:** the unchanged executable passes ten Linux threads versus batch10 on the same FPGA image, with seven samples of 40,960 requests and verified checksums, exit zero, usable shell, and clean poweroff. Median cycles are 3,270,749 demand-only versus 6,111,512 batched (86.85% more); ten resident workers remain unimplemented/unqualified, with only two resident/prefetch slots in the current image (`logs/linux-request-lifecycle-20260908/linux-reference10/`).
- **Isolated synthesis launcher hardening — 2026-09-10:** `feat/prefetch-queue-depth` now seeds nested `HardFloat` and `bedrock` URLs when preparing launch worktrees; this prevents detached run tree bootstrap failures when these submodules were not initialized. Commit `dc04ee0f` is pushed to origin. Host validation includes `make -C linux-tests request-benchmark-host check-request-benchmark` and a direct 10-worker run; the 10-worker run reports batched prefetch significantly faster than demand on x86 (`~3.33x` mean speedup in this capture). The `mt_prefetch_queue_depth_test` simulator run still returns PASS but is terminated by a known non-fatal `bsg_nonsynth_dpi_gpio` assertion (`%Fatal...final block executed before fini()`), so the make target exits 134 while guest/host checks pass.
- **Ten-logical-worker simulator benchmark — 2026-09-11:** added `mt_prefetch_nonresident_interleave_benchmark` for two resident banks and ten logical contexts. Fresh-boot demand, batched-ideal, and prefetch/yield/load runs pass with 359,870, 714, and 359,971 cycles respectively; the candidate is 0.03% slower than demand and about 503x slower than the ideal on this small cold-line test. The existing FPGA Linux ten-thread result remains a separate larger-workload reference.
- **High-latency overlap diagnosis — 2026-09-11:** the ten-worker simulator was rerun with the simulation-only pipelined AXI model (200-cycle reads, ten queue entries). Demand measured `0x114d21`, prefetch/yield/load `0x11696f` (1.8% slower), and batched ideal `0x9c9d`. The traced candidate accepted all ten hints but had `max_outstanding=1`; each hint completed before the next issue, because nonresident handoff traffic serializes through the UCE. More prefetch slots alone cannot fix this; overlap requires decoupling hint issue from the context-switch demand path or making the handoff path cheaper.
- **Instruction-refill contribution isolated — 2026-09-11:** the high-latency waveform shows approximately 3,655 cycles between accepted data hints. This interval is consistent with an eight-beat cold instruction-line refill plus nonresident handoff bookkeeping, so the current ten distinct worker entry points confound the data-prefetch experiment with instruction-cache misses. A shared-worker prototype was rejected after failing the ring correctness gate; the validated benchmark remains unchanged pending a safer hot-code redesign.
- **Prefetch benchmark fence audit — 2026-09-11:** removed the redundant hardware `fence r,r` after the inline hint. The hint remains a volatile assembly operation with a compiler memory clobber, and the UCE waveform records its accepted request; the fence cannot wait for completion and added roughly 257 cycles. The validated normal-model candidate still passes at `0x57e23` cycles.
- **Worker layout optimization — 2026-09-11:** removed forced 64-byte alignment from the ten static worker entry functions. The passing normal-model prefetch schedule improved to 356,578 cycles versus 356,493 demand (85-cycle, 0.024% gap); with 200-cycle reads, packed prefetch measured 1,121,534 versus 1,117,469 demand (0.36% gap). The optimization reduces cold instruction-refill overhead without changing the static context-switch protocol; all three schedules pass.
- **UCE overlap guard experiment — 2026-09-11:** temporarily permitted queued prefetch issue during both demand-read and uncached-read wait states and removed the older-credit drain condition. The ten-hint correctness gate passed, but the packed high-latency benchmark remained exactly `0x111cfe`; no overlap or performance change appeared. The RTL experiment was reverted, retaining the conservative production arbitration.
- **Worker-body reduction — 2026-09-11:** seeded each logical context with direct data/result pointers and removed per-worker address arithmetic and global-address materialization from the static worker body. Normal demand/prefetch now measure 355,874/355,908 cycles (34-cycle gap); the 200-cycle model measures 1,113,666/1,124,047 cycles. All three schedules pass; the change reduces benchmark overhead but does not create request overlap.
- **Switch-only control — 2026-09-11:** added `BENCH_MODE=3` to the ten-logical-worker simulator benchmark. It runs the same two-resident/ten-context continuation ring while omitting data loads, stores, and completion bookkeeping, and passes in `0x10a` (266) cycles. This is only about 0.075% of the 355,874-cycle demand run, confirming that the present gap is dominated by serialized cache/refill traffic rather than handoff instructions. A high-latency rebuild was allowed 120 seconds but did not complete before the simulator timeout; no result is claimed for that control.
- **Matched cache traffic accounting — 2026-09-11:** extended the waveform analyzer with `--allow-no-prefetch`, allowing demand-only traces to report traffic composition. Fresh ten-worker traces show demand at 370 normal UCE requests / 4,032 AXI bursts versus prefetch at 383 normal requests plus ten hints / 4,084 AXI bursts. The hint path is therefore observable, but both schedules remain strictly serialized (`max_outstanding=1`); the extra traffic explains the small regression rather than a compiler-elided hint.
- **Aggressive UCE overlap experiment — 2026-09-11:** permitting hints in every idle forward-pump cycle, including demand refill waits, fails the focused UCE regression because a matching demand can pass its pending hint. The change was reverted without benchmark or RTL claims; safe overlap requires explicit same-line ownership/response arbitration before widening issue eligibility.
- **Line-granular overlap and full-line hint experiments — 2026-09-11:** a temporary UCE change using cache-line granularity for matching-demand deferral passes the focused ownership regression and improves the normal ten-worker gap from 34 to 20 cycles, but high-latency tracing still reports `max_outstanding=1` and 1,124,035 prefetch versus 1,113,667 demand cycles. Expanding the hint from one word to a 64-byte request worsens the high-latency run to 1,137,979 cycles. Both RTL experiments were reverted; the current one-word hint remains the validated behavior.
- **Ordinary-credit bypass experiment — 2026-09-11:** allowing hints to issue even when the ordinary UCE credit count is full, with line-granular matching-demand deferral, passes the focused UCE regression but leaves the high-latency benchmark exactly unchanged at 1,124,035 cycles. The issue gate is therefore not the source of the observed serialization; the downstream L2 stream/cache controller remains the next boundary. The temporary RTL was reverted.
- **L2-bank placement control — 2026-09-11:** widened each benchmark record to a 128-byte stride to test whether adjacent 64-byte workers were colliding in the two-bank hash. High-latency demand/prefetch measured 1,120,386/1,131,535 cycles, still slower with hints and with a similar gap; placement alone is not the explanation. The benchmark layout was restored.
- **Hint-to-demand line correlation — 2026-09-11:** the waveform analyzer now reports normal L1 miss requests matching each accepted hint by 64-byte line. On the ten-worker trace, all ten hints have a subsequent same-line miss (`normal_miss_count=1` for every hint), directly proving that the current L2 advisory hint does not eliminate the later L1 miss.
- **L1-fill prototype — 2026-09-11:** temporarily routed prefetch requests through the ordinary UCE miss/refill path, captured dcache metadata, and expanded them to full-line reads so the existing tag/data fill machinery could install L1 state. The existing prefetch hint regression failed with corrupted/absent data and an unexpected trap; all changes were reverted. A correct L1 prefetch requires a dedicated response/retirement contract rather than reusing the blocking miss path.

- **Current nonresident ten-worker prefetch checkpoint — 2026-09-11:** refactored non-resident interleave benchmarking to compare demand, batched ideal, prefetched/yield, and switch-only schedules on two-resident/ten-logical contexts in one clean-tracked branch and retained it for simulator-first testing. A full clean+run sweep shows resident-control baseline and all modes pass, with no functional regressions and the known nonfatal `bsg_nonsynth_dpi_gpio` teardown assertion still present. Bench numbers are logged in branch run outputs: demand 355,874 cycles; prefetch/yield/load 355,757; batched ideal 668; and switch-only 266, with matched smoke and nonresident overhead tests also passing.
