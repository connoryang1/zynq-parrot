interface:
  display_name: "BlackParrot Synthesis Farm"
  short_description: "Run isolated FPGA builds across configured VMs"
  default_prompt: "Use $bp-synthesis-farm to probe builders, launch immutable BlackParrot FPGA candidates, monitor them, and collect accepted artifacts."
